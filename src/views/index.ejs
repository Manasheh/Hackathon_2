<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/css/index.css">
    <title>View Expenses</title>
    <style>
        * {
        margin: 0;
        padding: 0;
        }
       body {
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        font-family: Verdana, Geneva, Tahoma, sans-serif;
        /* align-items: center; */
        background-color: #17202A; 
        /* background-color: #709ccf;  */

       }
       .container {
        /* width: 40%; */
        /* min-height: 30%; */
        /* height: 30%; */
        display: flex;
        flex-direction: column;
        /* justify-content: center; */
        align-items: center;
        padding: 1.8rem;
       }
       h1 {
        font-size: 2.5rem;
        margin-bottom: 3rem;
        color: white;
       }
     
       table {
        width: 60%;
       }
       table thead th{
        font-size: 1.4rem;
        text-transform: uppercase;
        /* padding: 5px 10px; */
        background-color: aqua;
        padding: 0.6rem 2rem;
        /* border-top-right-radius: 10px; */
        /* border-top-left-radius: 10px; */
       }
      
       tbody tr td {
        font-size: 1.2rem;
        background-color: aliceblue;
        padding: 0.6rem 3rem;
        text-align: center;
       }
       .update, form .delete {
        text-decoration: none;
        color: rgb(45, 76, 232);
        font-size: 1rem;
        cursor: pointer;
       }
       form .delete {
        padding: 0.4rem 0.8rem;
        margin-top: 0.5rem;
        outline: none;
        border: none;
        background-color: rgb(218, 218, 240);
        color: red;
        border-radius: 5px;
       }
      
       .add_expense {
        color: black;
        text-decoration: none;
        padding: 0.7rem 2rem;
        background-color: white;
        border-radius: 6px;
        font-weight: 500;
        margin-top: 5px;
       }
       .foot {
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        /* margin-right: 3rem; */
       }
    </style>
</head>

<body>
    <div class="container">
    <h1>Expense List</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Item</th>
                <th>Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% expenses.forEach(expense => { %>
                <tr>
                    <td><%= expense.id %></td>
                    <td><%= expense.item %></td>
                    <td><%= expense.price %></td>
                    <td>
                        <a href="/update/<%= expense.id %>" class="update">Update</a>
                        <form action="/delete/<%= expense.id %>" method="post">
                            <button type="submit" class="delete">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }); %>
        </tbody>
        
    </table>
    <div class="foot">
    <nav>
        <a href="/add" class="add_expense">Add Expense</a>
    </nav>
    <div class="total-containre">
        <p class="total add_expense">Total : $ <%= total%></p>
    </div>
</div>
</div>
</body>
</html>


